server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/riskpilot
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    logging:
      level:
        root=INFO:

          app:
            test-mode: true